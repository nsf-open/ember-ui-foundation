{{yield (hash
  index          = (readonly this.rule.index)
  direction      = (readonly this.direction)
  nextDirection  = (readonly this.rule.nextDirection)
  displayName    = (readonly this.rule.displayName)
  iconClass      = (readonly this.iconClass)
  enabled        = (readonly this.rule.enabled)
  cycleDirection = this.cycleDirection
)}}